// A compiler validates code for balanced brackets (), {}, []. Build stack
// based validator. Extend: given daily stock prizes, compute Next Greater
// Price for each day (O(n) monotonic stack). Calculate stock span (how many
// consecutive previous days price was <= today).

public class StackBasedValidator {
    
}
